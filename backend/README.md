# @trade-blotter/backend

Fastify + TypeScript + Socket.IO REST/realtime API.

Responsibilities:
- REST endpoints for trade create / amend / cancel
- Postgres persistence via the Prisma client generated in `database`
- Socket.IO broadcast of trade events to connected blotter clients

> 🚧 Scaffolded — no application code yet.
