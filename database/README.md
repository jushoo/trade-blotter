# @trade-blotter/database

Prisma schema, migrations, and seed scripts for the trade blotter Postgres DB.

The generated Prisma Client is consumed by `@trade-blotter/backend`.

```
database/
└── prisma/
    ├── schema.prisma  # (todo) model definitions
    ├── migrations/     # (todo) generated migrations
    └── seed.ts         # (todo) seed script
```

> 🚧 Scaffolded — no schema or migrations yet.
